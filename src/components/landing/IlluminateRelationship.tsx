import { Container } from "@/components/ui/Container";
import { illuminateContent } from "@/content/illuminate";
import { event } from "@/config/event";

export function IlluminateRelationship(){return <section className="landing-section relationship" aria-labelledby="relationship-title"><Container><p className="text-eyebrow">Who is behind Illuminate?</p><h2 id="relationship-title">One initiative, hosted locally for MET students.</h2><div className="relationship__flow"><div><strong>{illuminateContent.roles.iitBombay.title}</strong><p>{event.association.label}.</p></div><span aria-hidden>↓</span><div><strong>Illuminate</strong><p>{illuminateContent.identity.description}</p></div><span aria-hidden>↓</span><div><strong>{illuminateContent.roles.met.title}</strong><p>E-Cell MET is hosting the MET edition and handling local registration, participant communication and event coordination.</p></div></div><p className="relationship__support">For local registration or communication support, contact {event.contacts.organizer.value} at <a href={`mailto:${event.contacts.support.value}`}>{event.contacts.support.value}</a>.</p></Container></section>}
